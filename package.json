{
  "name": "localsonly",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "node server",
    "build": "cd client/ && npm install && npm run build",
    "server": "node server/server.js ",
    "client": "npm start --prefix ./client",
    "dev": "concurrently \"nodemon server.js --prefix server\" \"npm start --prefix client\""
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^5.2.0"
  }
}
